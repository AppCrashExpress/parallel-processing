#ifndef RAY_H
#define RAY_H

#include "linear.h"

class Ray {
public:
    // Ray(){}
    // Ray(const Ray& r): pos(r.pos), dir(r.dir){}
    Vector3D pos;
    Vector3D dir;
    // bool, 
private:

};

#endif
